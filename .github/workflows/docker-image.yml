name: Docker Image CI

on:
  push:
    branches: [ "codespace-obscure-robot-4jr49494rgwr2x7" ]
  pull_request:
    branches: [ "codespace-obscure-robot-4jr49494rgwr2x7" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
